// In the asset pack’s build.gradle file:
plugins {
    id 'com.android.asset-pack'
}

assetPack {
    packName = "on_demand_pack" // Directory name for the asset pack
    dynamicDelivery {
        deliveryType = "on-demand"
    }
}

